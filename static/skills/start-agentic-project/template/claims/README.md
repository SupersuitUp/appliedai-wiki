# Claims

For projects that argue something. One file per claim. Delete this folder if the
project produces something that does not argue.

Copy `_example.md` as the shape. The deliverable then becomes a PROJECTION of
these files rather than a document you edit by hand: add ten sources and you
regenerate it instead of re-arguing it.
