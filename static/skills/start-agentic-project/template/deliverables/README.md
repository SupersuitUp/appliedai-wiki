# Deliverables

What gets handed over, GENERATED from the corpus rather than written here by hand.

Keep every version. Once the corpus is good the deliverable is cheap to
regenerate, so treat it as output. The corpus is the asset.

`check.py` counts anything here that is not a README or `_`-prefixed as a draft.
