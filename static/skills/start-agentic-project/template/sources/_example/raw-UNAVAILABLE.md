Use this file ONLY when you genuinely cannot get the source into the repo.

Say where it lives, why it is not here, and how a future session could get it.
A visible gap is fine. A silent one is not, which is why check.py treats a folder
with neither raw.* nor this file as not a source at all.
