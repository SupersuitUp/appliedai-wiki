# Hosted mirror

These files are served so they can be linked and pasted. They are a COPY.

Canonical: https://github.com/SupersuitUp/agentic-project-template
(except `SKILL.md`, which is the `start-agentic-project` skill and lives in
`~/.agents/skills/start-agentic-project/`).

Do not edit anything here. Change the canonical copy, tag it, then re-sync.
A working copy of this template diverged within a day of being made, which is
why every copy now says out loud that it is one.
